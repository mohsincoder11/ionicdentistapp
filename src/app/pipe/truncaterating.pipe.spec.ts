import { TruncateratingPipe } from './truncaterating.pipe';

describe('TruncateratingPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateratingPipe();
    expect(pipe).toBeTruthy();
  });
});
