import { AgeconvertPipe } from './ageconvert.pipe';

describe('AgeconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
